version: 2

updates:
  # The plugin has no runtime dependencies — it uses only the standard library —
  # so GitHub Actions is the only ecosystem with anything to track.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
